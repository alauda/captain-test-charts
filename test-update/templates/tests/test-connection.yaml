apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-update.fullname" . }}-test-connection"
  labels:
{{ include "test-update.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "test-update.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
